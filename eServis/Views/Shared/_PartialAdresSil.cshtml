
@model eServis.Entities.Adres

@{
    ViewBag.Title = "Delete";
}

@using (Html.BeginForm("Delete", "Adres", FormMethod.Post, new { id = "ListeForm" }))
{

    <div>

        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Baslik)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Baslik)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.Sira)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Sira)
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <button id="#btnDelete" class="btn btn-danger">Sil</button>


        }
    </div>
}
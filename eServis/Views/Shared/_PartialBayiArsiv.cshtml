
@model eServis.Entities.Bayi

@{
    ViewBag.Title = "Arşive Gönder";
}

@using (Html.BeginForm("Arsiv", "Bayi", FormMethod.Post, new { id = "ListeForm" }))
{

    <div>

        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Adi)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Adi)
            </dd>



        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <button id="#btnArsiv" type="submit" class="btn btn-danger">Arşive Gönder</button>


        }
    </div>
}

